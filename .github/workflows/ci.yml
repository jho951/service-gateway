name: CI

on:
  push:
    branches:
      - main
      - develop
      - "feature/**"
      - "fix/**"
      - "refactor/**"
      - "chore/**"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read

    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Grant execute permission for Gradle
        run: chmod +x ./gradlew

      - name: Compile Java
        run: ./gradlew --no-daemon clean compileJava

      - name: Build distribution
        run: ./gradlew --no-daemon installDist

      - name: Build Docker image
        run: DOCKER_BUILDKIT=1 docker build --secret id=gh_token,env=GH_TOKEN -f docker/Dockerfile -t api-gateway:ci .
